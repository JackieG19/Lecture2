#include<stdio.h>
#include<cs50.h>

int main(void)
{
    string name = get_string("Name: ");
    char initials[4];

    for (int i = 0; i = strlen(name); i++)
    {
        if (isupper(name[i]))
        {
            initials[1] = name[i];
        }
    }

}
